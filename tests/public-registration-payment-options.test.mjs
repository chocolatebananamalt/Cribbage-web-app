import assert from"node:assert/strict";import fs from"node:fs";import test from"node:test";import{isPublicRegistrationOptions}from"../src/lib/api/public-registration-options.ts";
test("registration options expose only accepted cash/check choices",()=>{assert.equal(isPublicRegistrationOptions({tournamentName:"Sample Tournament",acceptedMethods:["cash","check"]}),true);assert.equal(isPublicRegistrationOptions({tournamentName:"Sample Tournament",acceptedMethods:["venmo"]}),false);assert.equal(isPublicRegistrationOptions({tournamentName:"Sample Tournament",acceptedMethods:[]}),false);});
test("public options require the current unexpired registration credential",()=>{const sql=fs.readFileSync(new URL("../database/migrations/0157_tournament_payment_method_configuration.sql",import.meta.url),"utf8");assert.match(sql,/get_public_registration_payment_options_v1/);assert.match(sql,/fixed_32_byte_equal\(link\.token_digest,p_digest\)/);assert.match(sql,/link\.lifecycle_state='issued'/);assert.match(sql,/grant execute[\s\S]+to service_role/);const route=fs.readFileSync(new URL("../src/app/api/v1/registration/options/route.ts",import.meta.url),"utf8");assert.match(route,/isSameOriginRequest/);assert.match(route,/digestRegistrationLinkCredential/);assert.match(route,/get_public_registration_payment_options_v1/);});
