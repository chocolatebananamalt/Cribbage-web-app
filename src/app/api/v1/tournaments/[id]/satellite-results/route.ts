import { type NextRequest } from "next/server";
import { isSatelliteRejected, isSatelliteSaveRequest, isSatelliteSaved, isSatelliteWorkspace } from "../../../../../../lib/api/satellite-results";
import { apiJson, readLargeJson, requireVerifiedSubject, withApiFailureBoundary } from "../../../../../../lib/api/route-boundary";
import { isSameOriginRequest } from "../../../../../../lib/api/same-origin";
import { isUuid } from "../../../../../../lib/api/validation";
import { createServerOnlyAdminClient } from "../../../../../../lib/supabase/private-admin";
import { createClient } from "../../../../../../lib/supabase/server";
export async function GET(_request: NextRequest,{params}:{params:Promise<{id:string}>}){return withApiFailureBoundary(async()=>{const{id}=await params;if(!isUuid(id))return apiJson({error:"invalid_tournament"},{status:400});const actor=await requireVerifiedSubject(await createClient());if(!actor)return apiJson({error:"unauthorized"},{status:401});const{data,error}=await createServerOnlyAdminClient().rpc("get_satellite_results_workspace_v1",{p_actor_id:actor,p_tournament_id:id});if(error)return apiJson({error:"operation_unavailable"},{status:503});return isSatelliteWorkspace(data)?apiJson(data):apiJson({error:"not_found"},{status:404});});}
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){return withApiFailureBoundary(async()=>{if(!isSameOriginRequest(request))return apiJson({error:"invalid_origin"},{status:403});const{id}=await params;const body=await readLargeJson(request);if(!isUuid(id)||!isSatelliteSaveRequest(body))return apiJson({error:"invalid_satellite_results"},{status:400});const actor=await requireVerifiedSubject(await createClient());if(!actor)return apiJson({error:"unauthorized"},{status:401});const{data,error}=await createServerOnlyAdminClient().rpc("save_satellite_results_v1",{p_actor_id:actor,p_tournament_id:id,p_event_id:body.eventId,p_package_id:body.packageId,p_version_id:body.versionId,p_action:body.action,p_placements:body.placements,p_special_hands:body.specialHands,p_director_notes:body.directorNotes,p_expected_version:body.expectedVersion,p_idempotency_key:body.idempotencyKey});if(error)return apiJson({error:"operation_unavailable"},{status:503});if(isSatelliteSaved(data,body))return apiJson(data);if(isSatelliteRejected(data))return apiJson(data,{status:409});return apiJson({error:"operation_unavailable"},{status:503});});}
