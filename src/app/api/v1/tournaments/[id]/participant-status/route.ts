import{NextRequest}from"next/server";import{isParticipantStatusRequest,isParticipantStatusResult}from"../../../../../../lib/api/participant-status";import{apiJson,readLargeJson,requireVerifiedSubject,withApiFailureBoundary}from"../../../../../../lib/api/route-boundary";import{isSameOriginRequest}from"../../../../../../lib/api/same-origin";import{isUuid}from"../../../../../../lib/api/validation";import{createServerOnlyAdminClient}from"../../../../../../lib/supabase/private-admin";import{createClient}from"../../../../../../lib/supabase/server";
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){return withApiFailureBoundary(async()=>{if(!isSameOriginRequest(request))return apiJson({error:"invalid_origin"},{status:403});const{id}=await params;const body=await readLargeJson(request);if(!isUuid(id)||!isParticipantStatusRequest(body))return apiJson({error:"invalid_participant_status"},{status:400});const actor=await requireVerifiedSubject(await createClient());if(!actor)return apiJson({error:"unauthorized"},{status:401});const{data,error}=await createServerOnlyAdminClient().rpc("set_event_participant_status_v1",{p_actor_id:actor,p_tournament_id:id,p_event_id:body.eventId,p_participant_id:body.participantId,p_operation_kind:body.operationKind,p_reason:body.reason,p_idempotency_key:body.idempotencyKey});if(error)return apiJson({error:"operation_unavailable"},{status:503});return isParticipantStatusResult(data,body.participantId)?apiJson(data):apiJson(data&&typeof data==="object"?data:{error:"operation_unavailable"},{status:409});});}
