export type PublicRegistrationOptions={tournamentName:string;acceptedMethods:Array<"cash"|"check">};
export function isPublicRegistrationOptions(value:unknown):value is PublicRegistrationOptions {if(!value||typeof value!=="object"||Object.keys(value).length!==2)return false;const item=value as Record<string,unknown>;return typeof item.tournamentName==="string"&&item.tournamentName.trim().length>0&&item.tournamentName.length<=200&&Array.isArray(item.acceptedMethods)&&item.acceptedMethods.length>=1&&item.acceptedMethods.length<=2&&item.acceptedMethods.every((method)=>method==="cash"||method==="check")&&new Set(item.acceptedMethods).size===item.acceptedMethods.length;}
