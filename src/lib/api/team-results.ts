import { isUuid } from "./validation.ts";

export type TeamResultEntry={rank:number;teamEntryId:string;teamName:string;members:Array<{name:string;accNumber:string|null}>;gamePoints:number;gamesWon:number;plusSpreadPoints:number;minusSpreadPoints:number;netSpreadPoints:number;qualifies:boolean;qualificationStatus:"qualified"|"not_qualified"|"tie_review_required"|"not_applicable"};
export type TeamResults={tournamentName:string;eventId:string;eventName:string;format:"doubles"|"canadian_doubles";eventType:string;qualificationCount:number;qualificationBlocked:boolean;tieResolutionStatus:"not_required"|"pending_director_resolution"|"resolved";tieResolutionId:string|null;mrps:"not_applicable_satellite"|"eligible_after_review";entries:TeamResultEntry[]};
const object=(value:unknown):value is Record<string,unknown>=>!!value&&typeof value==="object"&&!Array.isArray(value);
const exact=(value:Record<string,unknown>,keys:string[])=>Object.keys(value).length===keys.length&&keys.every((key)=>key in value);
const nonnegative=(value:unknown)=>Number.isSafeInteger(value)&&(value as number)>=0;
export function isTeamResults(value:unknown):value is TeamResults{return object(value)&&exact(value,["tournamentName","eventId","eventName","format","eventType","qualificationCount","qualificationBlocked","tieResolutionStatus","tieResolutionId","mrps","entries"])&&typeof value.tournamentName==="string"&&isUuid(value.eventId)&&typeof value.eventName==="string"&&["doubles","canadian_doubles"].includes(String(value.format))&&typeof value.eventType==="string"&&nonnegative(value.qualificationCount)&&typeof value.qualificationBlocked==="boolean"&&["not_required","pending_director_resolution","resolved"].includes(String(value.tieResolutionStatus))&&(value.tieResolutionId===null||isUuid(value.tieResolutionId))&&["not_applicable_satellite","eligible_after_review"].includes(String(value.mrps))&&Array.isArray(value.entries)&&value.entries.every((entry)=>object(entry)&&exact(entry,["rank","teamEntryId","teamName","members","gamePoints","gamesWon","plusSpreadPoints","minusSpreadPoints","netSpreadPoints","qualifies","qualificationStatus"])&&Number.isSafeInteger(entry.rank)&&(entry.rank as number)>0&&isUuid(entry.teamEntryId)&&typeof entry.teamName==="string"&&Array.isArray(entry.members)&&entry.members.length===2&&entry.members.every((member)=>object(member)&&exact(member,["name","accNumber"])&&typeof member.name==="string"&&(member.accNumber===null||typeof member.accNumber==="string"))&&[entry.gamePoints,entry.gamesWon,entry.plusSpreadPoints,entry.minusSpreadPoints].every(nonnegative)&&Number.isSafeInteger(entry.netSpreadPoints)&&typeof entry.qualifies==="boolean"&&["qualified","not_qualified","tie_review_required","not_applicable"].includes(String(entry.qualificationStatus)));}
